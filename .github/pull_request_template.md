## What changed

<!-- One or two sentences. What does this do that the branch point did not? -->

## Why

<!-- The problem, not the patch. Link an ADR if this changes a decision. -->

## Timing impact

<!-- Anything touching the flash, the input path, or the frame loop needs a word
     here. "None" is a perfectly good answer, but say it out loud. -->

## Checks

- [ ] `npm test` passes locally
- [ ] `npm run lint` and `npm run typecheck` pass
- [ ] Decisions worth keeping are written down in `docs/adr/`
